import React from "react"

const Button = ({styles}) => (
    <button className={`bg-blue-gradient px-6 py-4 text-[18px] rounded-[10px] text-primary font-poppine font-medium outline-none ${styles}`}>
      Get Started
    </button>
  )


export default Button
